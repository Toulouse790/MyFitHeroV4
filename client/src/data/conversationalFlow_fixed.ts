
// Ajout en haut, à côté des autres fonctions :
function resolveNextStepPack(packId: string): string {
  return packId === 'custom' ? 'module_selection' : 'personal_info';
}


# => Le contenu complet avec corrections a été injecté ici par l'outil. Vous pouvez remplacer votre fichier.